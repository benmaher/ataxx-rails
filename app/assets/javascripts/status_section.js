(function( status_section, $, undefined ) {

  status_section.attach = function() {
    root_container = $('#status_section').get(0);
  }


}( window.status_section = window.status_section || {}, jQuery ));
